language: python
python:
  - "2.7"
  - "3.4"
  - "3.6"
  - "3.9"
branches:
  except:
    - piptools-ignore-patch
install:
  - "pip install -U setuptools pip wheel"
  - "pip install prysk"
  - "pip install ."
script:
  - "prysk --shell=/bin/bash tests/*.t"
